sudo apt update -y && sudo apt upgrade -y
sudo apt-get install unzip lsof ffmpeg build-essential -y
sudo apt-get install nginx libmysqlclient-dev supervisor socat -y
sudo apt-get update -y

sudo update-rc.d nginx defaults

wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh
rm Miniconda3-latest-Linux-x86_64.sh

ssh-keygen -t ed25519 -C "dhrvmohapatra@gmail.com"
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
cat ~/.ssh/id_ed25519.pub

git clone git@github.com:dhxmo/CoCa-cross-attn.git

cd CoCa-cross-attn/
conda create -n core python=3.10
conda activate core && pip install -r requirements.txt

aws configure 

--- add secret and access key

aws s3 cp s3://data-volume-test-rate/data_volumes/ -
